# ISS追尾システム - コード分析と改善案

**作成日**: 2026-07-09  
**対象**: E-ZEUS II赤道儀 + Python ASCOM制御  
**目的**: 構造分析と改善計画

---

## 📋 Executive Summary

### 現在の状態
- ✅ ISS軌道計算（Skyfield）: 実装完了
- ✅ シミュレーション環境: 完全動作
- ⚠️ ASCOM制御: 接続までは確認、実際の動作は未検証
- ❌ 実機統合: 未実施
- ⚠️ アーキテクチャ: 基本設計は良いが、ASCOM層に問題の可能性あり

### 主な問題点（疑い）
1. **ASCOM座標管理の問題** - RA/DECの単位混在、座標系の不一致
2. **接続状態管理** - エラーハンドリング不足、タイムアウト処理なし
3. **赤道儀動作の検証不足** - 移動命令が実際に届いているかスコープ内で未確認
4. **SUPER STAR IVとの情報統一** - アライメント情報の引き継ぎ方が不明確
5. **構造設計** - テストしにくい強密結合、レイアウト改善余地あり

---

## 🔍 詳細分析

### 1. ASCOM制御層の問題分析

#### 問題1: 座標単位の混乱
```python
# telescope.py - Line 77-78
ra = self.scope.RightAscension * 15.0  # hour → degree変換
dec = self.scope.Declination
```

**問題点:**
- ✅ 取得は正しい（hour → degree）
- ❌ 送信時を確認：

```python
# telescope.py - Line 113
self.scope.SlewToCoordinatesAsync(
    ra.hours,      # ← Skyfield Angle オブジェクト
    dec.degrees    # ← 単位を指定
)
```

**内部チェック:**
- `ra.hours`: Skyfield Angleの`.hours`プロパティ（float、0-24）
- `dec.degrees`: Skyfield Angleの`.degrees`プロパティ（float、-90-90）
- ASCOM仕様: 第1引数RA（hour単位、0-24）、第2引数Dec（degree単位、-90-90）

✅ **これは正しい**

#### 問題2: エラーハンドリングの不備

```python
# telescope.py - 問題: エラーハンドリング全くなし

def connect(self):
    chooser = win32com.client.Dispatch("ASCOM.Utilities.Chooser")
    # COMエラー時: 例外発生してそのまま終了
    progid = chooser.Choose(None)
    # Noneチェック: あり
    # しかし、Dispatch失敗: なし
    self.scope = win32com.client.Dispatch(progid)
    # Connected=False時の処理: なし
    self.scope.Connected = True
```

**改善が必要:**
- COM呼び出し例外の処理
- Connected失敗時の原因診断
- COMポート占有時の検出

#### 問題3: タイムアウト処理の欠落

```python
# telescope.py - wait_slew()
while self.scope.Slewing:
    if time.time() - start > timeout:
        raise TimeoutError("Slew timeout")
    time.sleep(interval)
```

**問題:**
- ✅ タイムアウト自体はある
- ❌ `Slewing`プロパティへのアクセス例外処理なし
- ❌ スレッド中止方法がない

#### 問題4: PulseGuideの実装が不完全

```python
# guider.py - pulse_guide()の実装
def guide(self, ra_velocity, dec_velocity, ra_error, dec_error):
    ra_rate = ra_velocity + ra_error * self.kp
    dec_rate = dec_velocity + dec_error * self.kp
    
    self.mount.pulse_guide(
        ra_rate,        # ← 度/秒か？
        dec_rate,       # ← 単位不明確
        0.05,           # ← ミリ秒？秒？
    )
```

**問題:**
- ❌ ASCOM PulseGuideの実装がない（telescope.pyに空のpulse_guide()メソッド）
- ❌ 単位が不明確（速度と継続時間の対応が不明）
- ❌ 方向定数(ASCOM GuideDirection)が不使用

**ASCOM PulseGuide仕様:**
```
PulseGuide(direction, duration_ms)
direction: 
  0=North, 1=South, 2=East, 3=West
duration_ms: パルス時間（ミリ秒）
```

**正しい実装例:**
```python
# RA速度が正なら東(East=2)、負なら西(West=3)
# Dec速度が正なら北(North=0)、負なら南(South=1)
direction = (2 if ra_rate >= 0 else 3)  # RA方向
direction = (0 if dec_rate >= 0 else 1)  # Dec方向: 両方同時は不可

# 速度 → 継続時間に変換する必要あり
# この変換ロジックがない！
```

#### 問題5: 接続状態の信頼性

```python
# telescope.py - is_connected()
def is_connected(self):
    if self.simulate:
        return True
    return self.scope is not None and self.scope.Connected
```

**問題:**
- ❌ `self.scope.Connected`へのアクセスで例外の可能性
- ❌ COMオブジェクトが無効化されている可能性を考慮していない

---

### 2. 座標系と変換の問題

#### 問題1: 赤道座標系の一貫性

**現在のフロー:**
```
ISS軌道(SKyfield: J2000 = 赤道座標系)
    ↓
Skyfield Position.ra, Position.dec (赤道座標)
    ↓
Telescope.get_position(): RA・Dec（度）
    ↓
Tracker: RA・Dec操作
    ↓
ASCOM: RA(hour)・Dec(degree)
```

**潜在的問題:**
- Skyfieldの座標系: J2000赤道座標（FK5）
- ASCOM期待座標系: 不明（通常J2000か視座標）
- E-ZEUS IIドライバの座標系: **不明**

**確認必要:**
- E-ZEUS IIドライバがどの座標系を使用するか
- 時刻の同期がSUPER STAR IVと一致しているか

#### 問題2: 地平座標系がコード内で全く使用されていない

```python
# models.py
@dataclass
class ISSPosition:
    # ...
    altitude: Angle    # 地平座標
    azimuth: Angle     # 地平座標
```

**これらは計算されているが、使用されていない！**

**理由:**
赤道儀はRA/Decで制御するため、地平座標は不要（正しい）

ただし:
- **検証用**: 地平座標の高度チェック（地表下なら見えない）
- **衝突検出**: 架台の可動範囲チェック
- **ロギング**: 実装した際の位置確認

---

### 3. ISS追尾設計の問題

#### 問題1: 速度ベースの追尾の課題

```python
# tracker.py - 現在の追尾ロジック
ra_velocity = (future_ra - iss_ra) / interval
dec_velocity = (future_dec - iss_dec) / interval

ra_rate = ra_velocity + ra_error * self.kp  # P制御
dec_rate = dec_velocity + dec_error * self.kp
```

**問題:**
- ✅ 予測追尾の基本は正しい
- ❌ 速度をPulseGuideのパルス時間に変換するロジックがない
- ❌ E-ZEUS IIのPulseGuide感度が不明（度/秒 vs 度/パルス）
- ❌ 微小時間(0.05秒)での速度計算：丸め誤差が大きい

#### 問題2: 時刻同期の危険性

```python
# tracker.py
elapsed = time.perf_counter() - start_real  # ← Windows時間
future_position = self.orbit.get_position_after(
    base_time,
    elapsed,  # ← UTC時刻に直接足している
)
```

**潜在的問題:**
- `perf_counter()`: プロセス起動時からの経過時間
- `get_position_after()`: UTS時刻に秒数を足す
- **ズレ:** Windows時刻とUTC同期の精度が鮮明でない

**例:**
```
プログラム開始時: 19:51:00 JST = 10:51:00 UTC
elapsed = 0.5秒
計算される時刻: 10:51:00.5 UTC

だが、実際のコンピュータ時刻がずれていたら？
```

#### 問題3: 架台の遅延が考慮されていない

現在の実装:
```python
# main.py
goto_delay = 20  # GoToに20秒かかると想定
target = orbit.get_position_after(
    skyfield_time,
    goto_delay  # ← 固定値
)
```

**問題:**
- ❌ 架台の実際のSlew速度に依存しない
- ❌ 目標までの距離に応じた遅延計算なし
- ❌ 大きく離れている場合、20秒では足りない可能性

---

### 4. ファイル構成と責務の問題

#### 現在の構成
```
iss/
├── config.py          ← 設定のみ
├── models.py          ← データモデルのみ
├── tle.py             ← TLE取得
├── orbit.py           ← ISS位置計算
├── predictor.py       ← 軌道予測
├── telescope.py       ← ASCOM制御
├── telescope_simulator.py ← シミュレータ
├── tracker.py         ← 追尾制御
├── guider.py          ← ガイド制御
├── mount_test.py      ← テストコード
├── check.py           ← テストコード（古い）
├── test.py            ← テストコード
└── app.py             ← シミュレータ用
```

#### 問題点

**問題1: テストコードが混在**
- `mount_test.py`, `check.py`, `test.py`: テストコード
- `app.py`: シミュレータのみで本プログラムと別

→ `tests/`フォルダに分離すべき

**問題2: 単一責務の原則（SRP）違反**

| ファイル | 責務 | 問題 |
|---------|------|------|
| telescope.py | ASCOM制御 | GOTOもPulseGuideもまとめ、テストしにくい |
| tracker.py | 追尾制御 | Mount依存で直結 |
| guider.py | 速度計算 | Mount依存で直結 |

**問題3: インターフェース化不足**

```python
# tracker.py
def __init__(self, orbit, mount, guider):
    self.mount = mount  # 型チェック全くなし
```

→ `mount`が`Telescope`でも`MountSimulator`でも動くのは偶然

→ 抽象クラス/プロトコルで定義すべき

**問題4: 責務が不明確**

- Guider: どこまでが「予測」で、どこからが「補正」か不明確
- Tracker: Guiderを呼ぶだけなのに、そもそもなぜ必要なのか？

---

### 5. 設計面での懸念

#### 懸念1: SUPER STAR IVとの関係

現在の実装:
```python
# main.py
# SUPER STAR IVで位置合わせ
# ↓ (ユーザー手動で)
# Python実行
telescope.connect()
telescope.goto(...)  # ← SUPER STAR IVでのアライメント情報を信頼
```

**問題:**
- ❌ SUPER STAR IVのアライメント情報がASCOMドライバに引き継がれるか未確認
- ❌ COMポート1つしかないため、切り替え時にCOMポート再初期化の可能性
- ❌ ドライバ側でアライメント状態がクリアされる可能性

**確認必要:**
- E-ZEUS IIドライバの設定画面でアライメント状態を確認
- ドライバが前回の接続状態を保持しているか

#### 懸念2: 移動命令の到達確認

現在:
```python
# telescope.py
def goto(self, ra, dec):
    self.scope.SlewToCoordinatesAsync(ra.hours, dec.degrees)
    # ← 呼び出しただけ、実際に赤道儀が動いたかどうか不明
```

**検証方法:**
1. ASCOM Driver設定画面で十字キー操作 → 赤道儀が動く？
2. `scope.CanMoveAxis`プロパティ確認
3. `scope.MoveAxis()`でテスト移動

---

## ✅ 確認すべき項目チェックリスト

### ハードウェア・ドライバ
- [ ] E-ZEUS IIドライバで十字キー操作 → 赤道儀実際に動く
- [ ] ASCOM Driver設定画面で「同期」可能か
- [ ] ドライバが座標系表示（J2000/視座標など）
- [ ] COMポート設定は正確か
- [ ] ドライバがアライメント情報を保持しているか

### SUPER STAR IVとの関係
- [ ] SUPER STAR IVで位置合わせ後、Pythonで座標取得 → 合致する
- [ ] COMポート切り替え後、座標がずれるか否か

### Skyfield
- [ ] TLE確認 (古すぎないか)
- [ ] de421.bspの有効期限
- [ ] 観測地点の緯度経度は正確か

---

## 🎯 改善案のアウトライン

### 即実装すべき改善

#### 1. **ASCOM層の補強**（優先度: 高）
- エラーハンドリング追加
- タイムアウト処理追加
- PulseGuideの正しい実装
- 接続状態の堅牢化

#### 2. **座標単位の明確化**（優先度: 高）
- 全コード内で単位を統一（内部: 度、入出力: 確認）
- 変換関数を作成（hour ↔ degree）
- ドキュメント化

#### 3. **構造リファクタリング**（優先度: 中）
- 抽象クラス/プロトコル導入
- テストコードを`tests/`に移動
- 関心の分離

#### 4. **時刻同期の改善**（優先度: 中）
- `datetime`ベースに統一
- `perf_counter()`ではなく`time.time()`か`datetime.now()`
- 時刻ズレの検出・補正

#### 5. **GoTo遅延の動的計算**（優先度: 中）
- `Slewing`状態を監視して実際のSlew時間を測定
- 次回以降、その遅延を使用

---

## 📊 改善の影響範囲

### ファイル依存関係
```
config.py
    ↓
models.py ← orbit.py ← skyfield (TLE)
    ↓
tle.py
    ↓
orbit.py ← predictor.py
    ↓            ↓
tracker.py ← guider.py
    ↓
telescope.py (ASCOM)
    ↓
main.py (統合)
```

### 修正時の注意
- `models.py`修正 → `orbit.py`, `predictor.py`に波及
- `telescope.py`修正 → `tracker.py`, `guider.py`, `main.py`に波及
- `config.py`修正 → すべてのファイルに波及（最小限に）

---

## 📝 まとめ

### 現状
- ✅ 基本設計は良い
- ✅ Skyfield統合は正しい
- ✅ シミュレーションは完全動作
- ❌ ASCOM層に信頼性問題
- ❌ テストしにくい構造
- ❌ エラーハンドリング不足

### 次ステップ
1. **検証**: ASCOM接続が実際に赤道儀を動かすか確認
2. **補強**: エラーハンドリング、タイムアウト処理追加
3. **リファクタリング**: 抽象化、構造改善
4. **実装**: 実機テスト、ISS追尾

